# Shadcn/ui 迁移实施计划

> 📅 制定日期：2025-09-23
> 🎯 目标：8-11小时内完成完整迁移
> 📊 难度评估：★★☆☆☆ (低难度)

## 执行概要

### 核心发现
✅ **项目已有90%的Shadcn基础**
- 已使用所有必需的Radix UI原语
- 已配置CVA和cn工具函数
- 已有components.json配置文件
- 组件结构已符合Shadcn标准

### 主要工作
🔄 **需要标准化样式系统**
- 更新43个组件到最新Shadcn样式
- 保留17个项目特有组件
- 统一主题变量系统

## 详细实施步骤

### 🚀 Day 1: 准备和高优先级组件（4-5小时）

#### 09:00-09:30 - 环境准备
```bash
# 1. 创建功能分支
git checkout -b feature/shadcn-migration-2025-09

# 2. 备份当前组件
cp -r components/ui components/ui.backup-$(date +%Y%m%d)

# 3. 创建迁移测试页面
mkdir app/shadcn-test
touch app/shadcn-test/page.tsx
```

#### 09:30-10:00 - 更新Shadcn配置
```bash
# 1. 更新配置文件
npx shadcn@latest init

# 选择配置:
# Style: new-york (保持当前)
# Base color: neutral
# CSS variables: Yes
```

#### 10:00-12:00 - 高优先级组件批量更新
```bash
# 批量更新核心组件（14个）
npx shadcn@latest add \
  button \
  card \
  dialog \
  form \
  input \
  label \
  select \
  table \
  tabs \
  textarea \
  alert \
  badge \
  dropdown-menu \
  sidebar \
  --overwrite
```

##### 更新后验证清单：
- [ ] Button - 测试所有变体（default/destructive/outline/secondary/ghost/link）
- [ ] Card - 检查布局和间距
- [ ] Dialog - 测试打开/关闭动画
- [ ] Form - 验证表单验证功能
- [ ] Input - 测试各种状态（disabled/error/focus）
- [ ] Table - 检查数据展示
- [ ] Tabs - 测试切换功能
- [ ] Alert - 验证各种类型
- [ ] Badge - 检查样式变体
- [ ] Dropdown - 测试交互

#### 12:00-13:00 - 午休

#### 13:00-14:00 - 测试和修复第一批组件
```typescript
// app/shadcn-test/page.tsx
// 创建测试页面，展示所有更新的组件
import { Button } from '@/components/ui/button'
import { Card } from '@/components/ui/card'
// ... 导入所有更新的组件

export default function TestPage() {
  return (
    <div className="container py-10">
      <h1>Shadcn组件测试</h1>
      {/* 测试各个组件 */}
    </div>
  )
}
```

### 🚀 Day 2: 中低优先级组件（3-4小时）

#### 09:00-11:00 - 中优先级组件更新
```bash
# 批量更新中等优先级组件（15个）
npx shadcn@latest add \
  accordion \
  calendar \
  carousel \
  checkbox \
  command \
  drawer \
  navigation-menu \
  pagination \
  popover \
  radio-group \
  sheet \
  switch \
  toast \
  toaster \
  tooltip \
  --overwrite
```

#### 11:00-12:00 - 低优先级组件微调
```bash
# 更新低优先级组件（12个）
npx shadcn@latest add \
  avatar \
  breadcrumb \
  progress \
  scroll-area \
  separator \
  skeleton \
  slider \
  toggle \
  toggle-group \
  resizable \
  chart \
  input-otp \
  --overwrite
```

### 🚀 Day 3: 集成和部署（2小时）

#### 09:00-10:00 - 全面集成测试
1. **功能测试**
   ```bash
   pnpm test:run
   pnpm test:e2e:basic
   ```

2. **性能测试**
   ```bash
   pnpm build
   # 检查包大小
   ```

3. **视觉回归测试**
   - 对比更新前后的UI截图
   - 确保没有意外的样式变化

#### 10:00-11:00 - 文档和部署
1. **更新文档**
   ```bash
   # 更新 README.md
   # 更新组件使用文档
   ```

2. **代码审查**
   ```bash
   git add .
   git commit -m "feat: Shadcn/ui 标准化迁移完成"
   git push origin feature/shadcn-migration-2025-09
   ```

3. **创建PR**
   ```bash
   gh pr create --title "feat: Shadcn/ui 组件标准化" \
     --body "## 更新内容\n- 标准化43个组件到最新Shadcn/ui\n- 保留17个项目特有组件\n- 统一主题系统"
   ```

## 组件迁移检查表

### 批次1：核心组件（2小时）
| 组件 | 更新前检查 | 更新命令 | 更新后验证 | 状态 |
|-----|----------|---------|----------|------|
| button | 备份样式 | ✅ | 测试变体 | ⏸️ |
| card | 备份结构 | ✅ | 测试布局 | ⏸️ |
| dialog | 备份交互 | ✅ | 测试动画 | ⏸️ |
| form | 备份验证 | ✅ | 测试提交 | ⏸️ |
| input | 备份状态 | ✅ | 测试输入 | ⏸️ |
| label | 备份样式 | ✅ | 测试关联 | ⏸️ |
| select | 备份选项 | ✅ | 测试下拉 | ⏸️ |
| table | 备份数据 | ✅ | 测试展示 | ⏸️ |
| tabs | 备份切换 | ✅ | 测试导航 | ⏸️ |
| textarea | 备份样式 | ✅ | 测试输入 | ⏸️ |
| alert | 备份类型 | ✅ | 测试显示 | ⏸️ |
| badge | 备份变体 | ✅ | 测试样式 | ⏸️ |
| dropdown-menu | 备份菜单 | ✅ | 测试交互 | ⏸️ |
| sidebar | 备份导航 | ✅ | 测试折叠 | ⏸️ |

### 批次2：交互组件（1.5小时）
| 组件 | 优先级 | 特殊注意事项 | 状态 |
|-----|--------|------------|------|
| accordion | 中 | 保留动画效果 | ⏸️ |
| calendar | 中 | 验证日期选择 | ⏸️ |
| carousel | 中 | 测试滑动效果 | ⏸️ |
| checkbox | 中 | 验证选中状态 | ⏸️ |
| command | 中 | 测试搜索功能 | ⏸️ |
| drawer | 中 | 验证滑出动画 | ⏸️ |
| navigation-menu | 中 | 测试导航逻辑 | ⏸️ |
| pagination | 中 | 验证分页逻辑 | ⏸️ |
| popover | 中 | 测试定位 | ⏸️ |
| radio-group | 中 | 验证单选逻辑 | ⏸️ |
| sheet | 中 | 测试侧边栏 | ⏸️ |
| switch | 中 | 验证切换 | ⏸️ |
| tooltip | 中 | 测试悬停 | ⏸️ |

### 批次3：展示组件（1小时）
| 组件 | 优先级 | 更新内容 | 状态 |
|-----|--------|---------|------|
| avatar | 低 | 样式微调 | ⏸️ |
| breadcrumb | 低 | 样式微调 | ⏸️ |
| progress | 低 | 样式微调 | ⏸️ |
| scroll-area | 低 | 保持现状 | ⏸️ |
| separator | 低 | 样式微调 | ⏸️ |
| skeleton | 低 | 保持现状 | ⏸️ |
| slider | 低 | 样式微调 | ⏸️ |
| toggle | 低 | 样式微调 | ⏸️ |
| toggle-group | 低 | 样式微调 | ⏸️ |

## 测试策略

### 单元测试
```bash
# 运行组件测试
pnpm test:run components/ui
```

### E2E测试
```bash
# 基础功能测试
pnpm test:e2e:basic

# 并发测试
pnpm test:e2e:concurrent
```

### 视觉测试
1. 截图对比工具
2. 暗色模式测试
3. 响应式测试

## 回滚策略

### 快速回滚
```bash
# 方案1: Git回滚
git checkout main
git branch -D feature/shadcn-migration-2025-09

# 方案2: 恢复备份
rm -rf components/ui
cp -r components/ui.backup-20250923 components/ui
```

### 部分回滚
```bash
# 仅回滚特定组件
npx shadcn@latest add [component-name] --overwrite
# 或从备份恢复单个文件
cp components/ui.backup-20250923/button.tsx components/ui/button.tsx
```

## 成功标准

### 技术指标
- ✅ 所有组件更新完成
- ✅ 测试覆盖率 > 80%
- ✅ 打包体积增长 < 10%
- ✅ 性能评分无下降

### 业务指标
- ✅ 无功能回归
- ✅ UI一致性提升
- ✅ 开发效率提升

## 风险矩阵

| 风险 | 概率 | 影响 | 缓解措施 |
|-----|------|------|----------|
| 样式冲突 | 中 | 低 | 逐个测试，保留备份 |
| 功能回归 | 低 | 中 | 完整测试覆盖 |
| 性能下降 | 低 | 低 | 性能监控 |
| 兼容性问题 | 低 | 中 | 浏览器测试 |

## 常见问题处理

### Q1: 组件样式不符合预期
```bash
# 解决方案：检查CSS变量
# 1. 查看 globals.css 中的变量定义
# 2. 对比 Shadcn 默认样式
# 3. 调整特定变量值
```

### Q2: 组件功能异常
```bash
# 解决方案：
# 1. 检查依赖版本
# 2. 查看控制台错误
# 3. 对比官方示例
```

### Q3: 打包体积增加过多
```bash
# 解决方案：
# 1. 检查 tree-shaking
# 2. 移除未使用的组件
# 3. 优化导入方式
```

## 联系和支持

- **技术问题**: 查看 [Shadcn/ui 官方文档](https://ui.shadcn.com)
- **项目问题**: 参考 [迁移指南](./shadcn-ui-migration-guide.md)
- **进度跟踪**: 查看 [迁移进度文档](./shadcn-migration-progress.md)

---

*计划制定：2025-09-23*
*执行周期：3个工作日*
*预计工时：8-11小时*