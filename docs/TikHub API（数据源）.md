抖音评论区数据获取与分析技术总结

  1. 整体架构概览

  评论区数据获取采用 Pipeline（管道）模式 + SSE流式处理 + LLM智能分析 的三层架构：

  用户输入 → 策略选择 → Pipeline执行 → TikHub API → LLM分析 → 实时流式输出

---
  2. 核心技术组件

  2.1 入口：策略选择系统 (app/api/chat/douyin-strategy.ts)

  - 设计模式：策略表（Strategy Table）模式，消除 if/else 堆叠
  - 工作原理：
    - 定义多个策略配置，每个策略包含：检测函数、Pipeline 函数、事件前缀、Token 配额
    - 按优先级匹配用户消息，自动选择对应的处理流程

  两大策略：
  1. VIDEO_EXTRACTION（视频文案提取）：优先级 1，需要关键词触发
  2. COMMENTS_ANALYSIS（评论分析）：优先级 2，纯分享链接默认走这个流程

  // 策略自动选择示例
  const strategy = selectDouyinStrategy(userMessage)
  if (strategy.name === 'COMMENTS_ANALYSIS') {
    // 执行评论分析 Pipeline
  }

---
  2.2 核心：评论分析 Pipeline (lib/douyin/comments-pipeline.ts)

  6步处理流程（comments-pipeline-steps.ts 定义）：

| 步骤                | 功能             | 关键技术                                        |
| ------------------- | ---------------- | ----------------------------------------------- |
| 1. parse-link       | 解析抖音分享链接 | 提取 videoId                                    |
| 2. fetch-detail     | 获取视频详情     | TikHub API: /fetch_one_video                    |
| 3. fetch-statistics | 获取播放数据     | TikHub API: /fetch_video_statistics             |
| 4. fetch-comments   | 采集评论数据     | TikHub API: /fetch_video_comments，支持自动分页 |
| 5. clean-comments   | 清理评论文本     | 移除表情符号 [xxx]，统计地域分布                |
| 6. analyze-comments | LLM 智能分析     | 调用 ZenMux API，流式输出分析结果               |

  关键特性：
  - 自动分页采集：默认采集 100 条评论（可配置 maxComments、maxPages）
  - 地域统计：自动聚合评论的 ip_label 字段，生成地域分布 Top 10
  - 流式输出：每个步骤实时发送进度事件，LLM 分析结果实时追加显示

---
  2.3 数据源：TikHub API 客户端 (lib/tikhub/client.ts)

  关键方法：
  // 1. 获取视频评论（支持分页）
  async getVideoComments(params: {
    aweme_id: string,
    cursor?: number,  // 分页游标
    count?: number    // 单页数量，默认20
  }): Promise<DouyinVideoCommentsResponse>

  // 2. 获取视频统计数据
  async getVideoStatistics(params: {
    aweme_ids: string  // 支持多个视频ID
  }): Promise<DouyinVideoStatisticsResponse>

  // 3. 获取视频详情
  async getVideoDetail(params: {
    aweme_id: string
  }): Promise<DouyinVideoDetailResponse>

  核心特性：
  - 熔断器保护：使用 CircuitBreaker 防止 API 雪崩
  - 自动重试：支持指数退避重试（maxRetries、retryDelay）
  - 错误处理：统一 HTTP 错误和业务错误处理

---
  2.4 LLM 分析引擎 (comments-pipeline.ts:182-338)

  分析维度（Prompt 设计）：
  1. 具体需求分析：用户询问的问题和明确表达的需求
  2. 用户画像：地域分布、年龄层、消费能力、身份特征
  3. 用户反馈问题：用户反映的痛点和问题

  技术实现：
  - API 选择：ZenMux API（替代 302.AI），支持 Claude/Gemini 等模型
  - 流式输出：使用 SSE（Server-Sent Events）实时推送分析片段
  - 数据构建：
    const analysisData = {
    video: { title, author },
    statistics: { play_count, digg_count, comment_count, ... },
    comments: [{ user, text, likes, location }, ...],
    locationStats: [{ location, count }, ...]
    }

  流式处理逻辑：
  // 处理 ZenMux 的流式响应格式
  if (line.startsWith('data:')) {
    const delta = data.choices?.[0]?.delta?.content
    if (delta) {
      fullText += delta
      // 实时发送分析片段给前端
      await emit({ type: 'partial', key: 'analysis', data: delta, append: true })
    }
  }

---
  2.5 API 端点 (app/api/douyin/analyze-comments/route.ts)

  路由：POST /api/douyin/analyze-comments

  流程：
  1. 认证检查（NextAuth JWT Token）
  2. 解析请求参数（shareLink）
  3. 创建 SSE 流：
     - event: comments-progress  // 进度更新
     - event: comments-info      // 视频信息
     - event: comments-partial   // LLM 分析片段
     - event: comments-done      // 完成
     - event: comments-error     // 错误
  4. 执行 runDouyinCommentsPipeline()
  5. 返回流式响应（Content-Type: text/event-stream）

---
  2.6 前端展示组件 (components/chat/douyin-comments-progress.tsx)

  技术架构：
  - 骨架组件：PipelineProgressSkeleton 统一进度展示样式
  - 实时更新：通过 DouyinCommentsProgressState 管理状态
    - progress.videoInfo：视频信息（标题、作者、播放量、点赞数、评论数）
    - progress.statistics：统计数据
    - progress.analysisPreview：LLM 分析实时预览

  展示布局：
  📊 进度条（0-100%）
  ├─ 步骤1: 解析链接 ✓
  ├─ 步骤2: 获取视频信息 ✓
  ├─ 步骤3: 获取播放数据 ✓
  ├─ 步骤4: 采集评论（已采集 X 条）⏳
  ├─ 步骤5: 清理评论 ⏳
  └─ 步骤6: AI 智能分析 ⏳

  📹 视频信息卡片
  - 标题、作者
  - 播放量、点赞、评论数

  🤖 AI 分析实时预览
  - 流式显示 LLM 输出

---
  3. 数据流转示意图

  ┌───────────────────┐
  │   用户输入链接    │
  │ (商家详情页点击)  │
  └─────────┬─────────┘
            │
            ▼
  ┌───────────────────┐
  │  策略选择系统      │ (douyin-strategy.ts)
  │  检测类型 → 策略   │
  └─────────┬─────────┘
            │
            ▼
  ┌───────────────────────────────────────────────┐
  │         评论分析 Pipeline                      │ (comments-pipeline.ts)
  │  ┌────────────────────────────────────────┐  │
  │  │ 1. parseDouyinVideoShare               │  │
  │  │    ↓ 提取 videoId                      │  │
  │  │ 2. tikhubClient.getVideoDetail()       │  │
  │  │    ↓ 获取视频信息                      │  │
  │  │ 3. tikhubClient.getVideoStatistics()   │  │
  │  │    ↓ 获取播放数据                      │  │
  │  │ 4. tikhubClient.getVideoComments()     │  │
  │  │    ↓ 分页采集评论（最多 100 条）       │  │
  │  │ 5. cleanCommentText()                  │  │
  │  │    ↓ 清理文本 + 地域统计               │  │
  │  │ 6. analyzeWithLLM()                    │  │
  │  │    ↓ ZenMux API 流式分析               │  │
  │  └────────────────────────────────────────┘  │
  └───────────────┬───────────────────────────────┘
                  │
                  ▼
  ┌───────────────────────────────────────────────┐
  │         SSE 流式输出                           │
  │  event: comments-progress → 进度条更新         │
  │  event: comments-info     → 视频信息卡片       │
  │  event: comments-partial  → AI 分析实时显示    │
  │  event: comments-done     → 完整 Markdown 报告 │
  └───────────────┬───────────────────────────────┘
                  │
                  ▼
  ┌───────────────────────────────────────────────┐
  │       前端组件实时渲染                         │ (douyin-comments-progress.tsx)
  │  - 进度条动画                                  │
  │  - 视频信息展示                                │
  │  - AI 分析逐字显示                             │
  └───────────────────────────────────────────────┘

---
  4. 关键技术亮点

  ✅ 事件驱动架构

  - Pipeline 通过 emit() 函数发送 5 种事件类型
  - 前端订阅 SSE 流，实时更新 UI 状态

  ✅ 流式 LLM 分析

  - 使用 ReadableStream 处理 ZenMux 的流式响应
  - 实时追加显示分析文本（append: true）

  ✅ 自动分页采集

  - while (has_more && pageCount < maxPages) 循环采集
  - 单页失败不中断流程，继续采集下一页

  ✅ 地域统计

  - 使用 Map<string, number> 聚合评论 IP 归属地
  - 排序后取 Top 10 展示地域分布

  ✅ 熔断器保护

  - TikHub API 客户端内置熔断器（CircuitBreaker）
  - 防止 API 故障雪崩，自动降级

  ✅ 统一错误处理

  - DouyinCommentsPipelineStepError 包装所有错误
  - 精确定位失败步骤，便于调试

---
  5. 环境配置要求

  # TikHub API（数据源）
  TIKHUB_API_KEY=your_tikhub_key

  # ZenMux API（LLM 分析）
  ZENMUX_API_BASE=https://zenmux.ai/api/v1
  ZENMUX_API_KEY=your_zenmux_key
  ZENMUX_DEFAULT_MODEL=anthropic/claude-sonnet-4.5

---
  6. 与商家详情页的集成路径

  目前调研发现：
  - 商家详情页（app/merchants/[id]/page.tsx）：展示商家的内容列表，每个内容项包含 shareUrl
  - 评论分析功能：目前是通过聊天对话调用，用户需要手动粘贴分享链接

  潜在集成点：
  1. 在内容列表的每个视频卡片上添加"分析评论"按钮
  2. 点击后触发评论分析 Pipeline
  3. 在 Modal 或侧边栏中展示 DouyinCommentsProgress 组件
  4. 分析完成后，将结果保存到数据库（可选）

---
  7. 技术债务和改进方向

  🔴 当前限制

  - 评论采集数量上限：100 条（可配置，但受 TikHub API 限流影响）
  - 评论文本清理：简单的正则替换，可能误删有用信息
  - LLM 分析成本：每次分析消耗 ~4000 tokens

  🟢 改进建议

  1. 批量分析：商家详情页批量选择多个视频，一次性分析所有评论
  2. 结果缓存：将分析结果存入 MerchantContent 表的 metadata 字段
  3. 评论过滤：支持按点赞数、关键词过滤评论
  4. 词云分析：调用 TikHub 的 fetch_hot_comment_word_list API，生成评论词云
  5. 情感分析：LLM 分析结果结构化存储（sentiment、coreTopics、userProfile、suggestions）

---
  🎯 总结

  评论区数据获取和分析是一个完整的 Pipeline + SSE + LLM 三层架构：

  1. Pipeline 层：6 步流程自动化采集和清理数据
  2. API 层：TikHub 提供数据源，ZenMux 提供 LLM 分析
  3. 前端层：实时流式展示进度和分析结果

  所有核心代码都在 lib/douyin/comments-pipeline.ts，遵循 事件驱动 + 流式处理 设计模式，易于扩展和维护。