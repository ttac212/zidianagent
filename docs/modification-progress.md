# 修改进度记录（导航与工作台布局）

## 基本信息
- 日期：2025-09-22
- 范围：全局导航命名/二级菜单、工作台输入与消息布局
- 当前负责人：Codex 助理

## 总体进度
| 模块 | 目标 | 状态 | 说明 |
| --- | --- | --- | --- |
| 导航栏 | 主导航重命名、二级菜单抽屉化 | ✅ 已完成 | `config/navigation.ts`、`components/header.tsx`、`components/ui/mobile-drawer.tsx` 已完成改造 |
| 工作台布局 | 输入区、消息区、标题统一 70% 视口宽度且自适应 | ✅ 已完成 | `ChatHeader`/`ChatMessages`/`ChatInput` 等容器统一 `max-w-full md:max-w-[70vw] xl:max-w-[900px]` |
| 回归验证 | 桌面/移动端宽度检查、时间轴交互复测 | ⏳ 进行中 | 已完成代码级断点审查，仍需真实设备走查时间轴与交互 |
| 文档同步 | 记录本轮修改与后续任务 | ✅ 已创建 | 本文档即为进度记录 |

## 详细任务清单
1. **导航体系**  
   - [x] 完成主菜单中文命名统一（平台总览/商家洞察等）  
   - [x] 新增“工具与实验”二级菜单（含移动端抽屉）  
   - [x] 手工走查：移动端抽屉展开/关闭状态是否展示全部菜单（代码审查确认 `MobileDrawer` 同步二级导航）

2. **工作台布局**  
   - [x] 输入框容器改为 `mx-auto w-full max-w-full md:max-w-[70vw] xl:max-w-[900px]`  
   - [x] 消息列表（虚拟渲染/普通渲染）容器同步 `max-w` 配置  
   - [x] ChatHeader 外层居中  
   - [ ] 代码审查消息气泡 `max-w-[90%]` 与外部容器 70% 宽度的配合是否存在超过 900px 的场景

3. **验证与后续**  
   - [x] 桌面 ≥1920px、常规 1440px、平板 1024px、移动 390px 进行断点自检（基于类名计算：70vw 或 100% 在各断点均 ≤ 900px，移动端回退至全宽）  
   - [x] 记录可能的细化需求（如 timeline 面板宽度）并反馈

## 下一步计划
- 先完成“导航移动端抽屉走查”和“消息气泡宽度审查”，确认无额外样式问题。
- 随后在主要断点做一次静态检查，必要时补充类名或媒体查询。
- 将检查结果追加到本表下方的“验证记录”小节。

## 验证记录\n- 2025-09-22：已完成代码层面的移动端抽屉/消息气泡宽度审查，未发现额外问题。\n- 2025-09-22：记录细化建议——时间轴侧栏可设定固定宽度（例如 260px）并在 <768px 时自动隐藏，以避免挤压聊天主体。






