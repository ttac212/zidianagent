# 多模型使用量统计功能实现进度文档

## 📋 项目概述

**项目名称：** 多模型使用量统计功能实现  
**创建时间：** 2025-08-29  
**目标：** 实现Claude和Gemini两个模型的分离使用量统计  
**当前状态：** 🟡 准备阶段 - 已完成现状分析  

## 🎯 功能目标

### 核心需求
1. **分模型统计：** 区分Claude和Gemini的Token使用量
2. **数据展示：** 在Settings页面显示各模型的使用统计
3. **历史追踪：** 支持按日期查看各模型的使用趋势
4. **兼容性：** 保持现有多KEY架构不变

### 成功标准
- ✅ 数据库能按模型记录使用量统计
- ✅ Settings页面显示分模型的使用量卡片
- ✅ 支持Claude和Gemini使用量的独立统计
- ✅ 历史数据不丢失，新旧统计并存

## 🔍 技术现状分析

### ✅ 现有优势
1. **多KEY架构完善** - `lib/ai/key-manager.ts`已支持按模型选择API Key
2. **Message表已就绪** - 每条消息已记录`modelId`字段  
3. **基础统计逻辑** - `/api/chat`已实现Token统计
4. **前端框架** - Settings页面已有统计展示基础

### ❌ 需要解决的问题
1. **UsageStats表缺少模型维度** - 只有总量统计，无模型分组
2. **API统计逻辑不完整** - 更新UsageStats时未按模型分组记录
3. **前端展示单一** - Settings页面只显示总体统计

## 📅 详细实施计划

### 阶段1：数据库结构扩展 🔧
**预估时间：** 2-3小时  
**负责模块：** 数据库层  

#### 1.1 扩展UsageStats模型
**文件：** `prisma/schema.prisma`
**操作：** 在UsageStats模型中添加模型相关字段

```prisma
model UsageStats {
  // ... 现有字段保持不变
  
  // 新增字段
  modelId         String?              // 模型ID（可选，兼容历史数据）
  modelProvider   String?              // 模型提供商（Claude/Google）
  
  // 更新索引
  @@unique([userId, date])             // 保留原有约束（兼容性）
  @@unique([userId, date, modelId])    // 新增按模型的唯一约束
  @@index([modelId, date])             // 按模型查询优化
  @@index([modelProvider, date])       // 按提供商查询优化
}
```

#### 1.2 创建数据库迁移
**命令：** `npx prisma migrate dev --name add-model-stats`
**文件生成：** `prisma/migrations/*/migration.sql`

#### 1.3 更新Prisma客户端
**命令：** `npx prisma generate`

**进度状态：** ⬜ 未开始

---

### 阶段2：API统计逻辑优化 ⚙️
**预估时间：** 3-4小时  
**负责模块：** 后端API  

#### 2.1 修改聊天API统计逻辑
**文件：** `app/api/chat/route.ts`
**位置：** 第346-372行 UsageStats更新部分

**现有逻辑：**
```typescript
await prisma.usageStats.upsert({
  where: { userId_date: { userId: userId, date: today } },
  // 只更新总量统计
})
```

**目标逻辑：**
```typescript
// 1. 更新总量统计（保持兼容）
await prisma.usageStats.upsert({
  where: { userId_date: { userId: userId, date: today } },
  // 现有逻辑保持不变
})

// 2. 更新按模型分组统计（新增）
const keySelection = selectApiKey(useModel) // 已有
await prisma.usageStats.upsert({
  where: { 
    userId_date_modelId: { 
      userId: userId, 
      date: today, 
      modelId: useModel 
    } 
  },
  update: {
    // 按模型更新统计
  },
  create: {
    // 按模型创建统计
  }
})
```

#### 2.2 提取模型信息工具函数
**新文件：** `lib/ai/model-stats-helper.ts`
**功能：** 提取模型提供商信息

```typescript
export function getModelProvider(modelId: string): string {
  if (modelId.startsWith('claude-')) return 'Claude'
  if (modelId.startsWith('gemini-')) return 'Google'
  return 'Unknown'
}
```

**进度状态：** ⬜ 未开始

---

### 阶段3：统计查询API开发 📊
**预估时间：** 2-3小时  
**负责模块：** API端点  

#### 3.1 扩展用户统计API
**文件：** `app/api/users/[id]/route.ts`
**修改位置：** 第42-50行 usageStats查询部分

**现有查询：**
```typescript
usageStats: {
  select: {
    date: true,
    totalTokens: true,
    apiCalls: true,
  },
  orderBy: { date: 'desc' },
  take: 30,
}
```

**目标查询：**
```typescript
usageStats: {
  select: {
    date: true,
    totalTokens: true,
    apiCalls: true,
    modelId: true,          // 新增
    modelProvider: true,    // 新增
  },
  orderBy: { date: 'desc' },
  take: 90, // 增加数量，因为有模型分组
}
```

#### 3.2 创建模型统计聚合API
**新文件：** `app/api/users/[id]/model-stats/route.ts`
**功能：** 提供按模型聚合的统计数据

```typescript
// GET /api/users/:id/model-stats
// 返回格式：
{
  totalStats: {
    totalTokens: number,
    totalRequests: number
  },
  modelStats: {
    "claude-opus-4-1-20250805": {
      totalTokens: number,
      requests: number,
      percentage: number
    },
    "gemini-2.5-pro": {
      totalTokens: number,
      requests: number,
      percentage: number
    }
  },
  dailyStats: Array<{
    date: string,
    models: Record<string, {tokens: number, requests: number}>
  }>
}
```

**进度状态：** ⬜ 未开始

---

### 阶段4：前端UI开发 🎨
**预估时间：** 4-5小时  
**负责模块：** 前端展示  

#### 4.1 创建模型统计卡片组件
**新文件：** `components/stats/model-usage-cards.tsx`
**功能：** 显示各模型使用量的卡片组件

```typescript
interface ModelUsageCardsProps {
  modelStats: ModelStatsData
  loading: boolean
}

export function ModelUsageCards({ modelStats, loading }: ModelUsageCardsProps) {
  // 渲染Claude和Gemini的使用量卡片
  // 支持加载状态
  // 显示使用量、百分比、趋势
}
```

#### 4.2 创建模型使用量对比图表
**新文件：** `components/stats/model-usage-chart.tsx`
**依赖：** recharts（已安装）
**功能：** 显示各模型历史使用趋势

#### 4.3 更新Settings页面
**文件：** `app/settings/page.tsx`
**修改位置：** 第150-191行 用量统计区域

**新增内容：**
```typescript
// 导入新组件
import { ModelUsageCards } from '@/components/stats/model-usage-cards'
import { ModelUsageChart } from '@/components/stats/model-usage-chart'

// 新增状态
const [modelStats, setModelStats] = useState(null)

// 新增数据获取
const fetchModelStats = async () => {
  const res = await fetch(`/api/users/${userId}/model-stats`)
  const data = await res.json()
  setModelStats(data)
}

// 在原有统计卡片下方添加模型统计
<ModelUsageCards modelStats={modelStats} loading={loadingUsage} />
<ModelUsageChart modelStats={modelStats} loading={loadingUsage} />
```

**进度状态：** ⬜ 未开始

---

### 阶段5：加载动画优化 ✨
**预估时间：** 1-2小时  
**负责模块：** UI体验  

#### 5.1 扩展骨架加载组件
**文件：** `components/ui/stats-card-skeleton.tsx`
**新增：** 模型统计卡片的骨架动画

```typescript
export const ModelStatsGridSkeleton = () => {
  return (
    <div className="grid grid-cols-1 md:grid-cols-2 gap-4">
      <StatsCardSkeleton /> {/* Claude */}
      <StatsCardSkeleton /> {/* Gemini */}
    </div>
  )
}
```

**进度状态：** ⬜ 未开始

---

### 阶段6：测试与验证 🧪
**预估时间：** 2-3小时  
**负责模块：** 质量保证  

#### 6.1 数据库测试
- [ ] 验证UsageStats新字段正常记录
- [ ] 验证历史数据兼容性
- [ ] 验证模型分组统计准确性

#### 6.2 API测试  
- [ ] 测试聊天API的统计记录
- [ ] 测试模型统计API数据准确性
- [ ] 测试不同模型的使用量分离

#### 6.3 前端测试
- [ ] 验证统计卡片显示正确
- [ ] 验证加载状态流畅
- [ ] 验证响应式布局

**进度状态：** ⬜ 未开始

---

## 📊 进度跟踪表

| 阶段 | 任务 | 状态 | 开始时间 | 完成时间 | 备注 |
|-----|------|------|----------|----------|------|
| 1.1 | 扩展UsageStats模型 | ⬜ 未开始 | | | |
| 1.2 | 创建数据库迁移 | ⬜ 未开始 | | | |
| 1.3 | 更新Prisma客户端 | ⬜ 未开始 | | | |
| 2.1 | 修改聊天API统计逻辑 | ⬜ 未开始 | | | |
| 2.2 | 提取模型信息工具函数 | ⬜ 未开始 | | | |
| 3.1 | 扩展用户统计API | ⬜ 未开始 | | | |
| 3.2 | 创建模型统计聚合API | ⬜ 未开始 | | | |
| 4.1 | 创建模型统计卡片组件 | ⬜ 未开始 | | | |
| 4.2 | 创建模型使用量对比图表 | ⬜ 未开始 | | | |
| 4.3 | 更新Settings页面 | ⬜ 未开始 | | | |
| 5.1 | 扩展骨架加载组件 | ⬜ 未开始 | | | |
| 6.1 | 数据库测试 | ⬜ 未开始 | | | |
| 6.2 | API测试 | ⬜ 未开始 | | | |
| 6.3 | 前端测试 | ⬜ 未开始 | | | |

**状态说明：**
- ⬜ 未开始
- 🟡 进行中
- ✅ 已完成
- ❌ 已阻塞

## ⚠️ 风险点与注意事项

### 数据兼容性风险
**风险：** 扩展UsageStats表可能影响现有数据
**缓解措施：**
- 新增字段设置为可选（nullable）
- 保留原有唯一约束
- 创建迁移脚本前做好数据备份

### 性能影响
**风险：** 按模型分组统计可能增加数据库负载
**缓解措施：**
- 合理设计索引
- 统计查询增加缓存
- 监控查询性能

### 多KEY架构影响
**风险：** 统计逻辑修改可能影响现有多KEY架构
**缓解措施：**
- 复用现有`selectApiKey`函数
- 不修改多KEY选择逻辑
- 在统计层面添加模型信息

## 🔍 验收标准

### 功能验收
1. **数据分离：** Claude和Gemini使用量能独立统计
2. **界面展示：** Settings页面显示分模型统计卡片
3. **数据准确：** 统计数据与实际使用一致
4. **兼容性：** 历史数据和现有功能不受影响

### 性能验收  
1. **加载速度：** 统计页面加载时间<2秒
2. **API响应：** 统计API响应时间<500ms
3. **数据库：** 查询不影响聊天功能性能

### 用户体验验收
1. **加载动画：** 统计数据加载过程有适当动画
2. **响应式：** 在移动端和桌面端都有良好展示
3. **信息清晰：** 用户能直观了解各模型使用情况

## 📝 开发备注

### 关键代码文件清单
```
修改文件：
- prisma/schema.prisma (UsageStats扩展)
- app/api/chat/route.ts (统计逻辑)
- app/api/users/[id]/route.ts (查询扩展)
- app/settings/page.tsx (前端展示)
- components/ui/stats-card-skeleton.tsx (加载动画)

新建文件：
- lib/ai/model-stats-helper.ts (工具函数)
- app/api/users/[id]/model-stats/route.ts (统计API)
- components/stats/model-usage-cards.tsx (统计卡片)
- components/stats/model-usage-chart.tsx (统计图表)
```

### 测试数据准备
创建测试用例时需要准备：
- 包含Claude和Gemini消息的对话数据
- 不同日期的使用量统计数据
- 边界情况测试数据（空数据、单模型数据等）

---

**文档版本：** v1.0  
**最后更新：** 2025-08-29  
**下次更新：** 开始实施后及时更新进度状态