# TikHub API 低粉爆款榜测试工具

## 功能说明

这个测试工具用于测试 TikHub API 的低粉爆款榜接口，获取抖音各垂类的低粉爆款视频排行榜。

### API 信息
- **端点**: `/api/v1/douyin/billboard/fetch_hot_total_low_fan_list`
- **方法**: POST
- **认证**: Bearer Token (使用 `TIKHUB_API_KEY`)
- **参数**:
  - `page`: 页码，默认1
  - `page_size`: 每页数量，默认10
  - `date_window`: 时间窗口，1按小时 2按天，默认24
  - `tags`: 子级垂类标签，空则为全部
- **返回**: 低粉爆款视频榜单列表（按热度排序）

### 什么是低粉爆款？

低粉爆款是指粉丝数量较少（通常几千到几万），但视频播放量、互动量很高的内容。这类内容往往代表：
- 新人创作者的成功案例
- 优质内容的突破口
- 垂类趋势的早期信号
- 创作灵感的重要来源

## 使用方法

### 1. 基本使用 - 获取全部垂类低粉爆款榜

```bash
npx tsx scripts/test-low-fan-list.ts
```

**输出内容包括**:
- 视频标题
- 作者昵称
- 播放数、点赞数、粉丝数、关注数
- 热度值
- 发布时间
- 视频ID
- 完整数据导出到 JSON 文件

### 2. 获取特定垂类的低粉爆款榜

```bash
# 美食垂类
npx tsx scripts/test-low-fan-list.ts --tag=628

# 旅行垂类
npx tsx scripts/test-low-fan-list.ts --tag=629

# 教育校园垂类
npx tsx scripts/test-low-fan-list.ts --tag=626
```

### 3. 自定义参数

```bash
# 查询按小时窗口的数据，每页20条
npx tsx scripts/test-low-fan-list.ts --window=1 --size=20

# 美食垂类，按天窗口，5条数据
npx tsx scripts/test-low-fan-list.ts --tag=628 --window=24 --size=5
```

## 请求参数结构

### 全部垂类查询
```json
{
  "page": 1,
  "page_size": 10,
  "date_window": 24,
  "tags": []
}
```

### 单个垂类查询（如美食）
```json
{
  "page": 1,
  "page_size": 10,
  "date_window": 24,
  "tags": [
    { "value": 628 }
  ]
}
```

### 垂类+子垂类查询
```json
{
  "page": 1,
  "page_size": 10,
  "date_window": 24,
  "tags": [
    {
      "value": 628,
      "children": [
        { "value": 62804 },
        { "value": 62803 }
      ]
    }
  ]
}
```

## 响应数据结构

### 视频对象
```typescript
interface LowFanVideoInfo {
  item_id: string              // 视频ID
  item_title: string           // 视频标题
  item_cover_url: string       // 封面URL
  item_duration: number        // 视频时长(毫秒)
  nick_name: string            // 作者昵称
  avatar_url: string           // 作者头像URL
  fans_cnt: number             // 粉丝数
  play_cnt: number             // 播放数
  publish_time: number         // 发布时间戳
  score: number                // 热度值
  item_url: string             // 视频URL
  like_cnt: number             // 点赞数
  follow_cnt: number           // 关注数
  follow_rate: number          // 关注率
  like_rate: number            // 点赞率
  media_type: number           // 媒体类型
  rank?: number                // 排名
}
```

### 示例响应
```json
{
  "code": 0,
  "data": {
    "page": {
      "page": 1,
      "page_size": 5,
      "total": 1000
    },
    "objs": [
      {
        "item_id": "7572586014012717994",
        "item_title": "他家👟真靠谱👉@潮鞋聚友（🉐档口） 过个纪念日男朋友的头发被火烧了。",
        "nick_name": "Yaooo.",
        "play_cnt": 111227583,
        "like_cnt": 1841015,
        "fans_cnt": 12824,
        "score": 4155846,
        "publish_time": 1763130079
      }
    ]
  }
}
```

## 前置条件

### 1. 环境变量配置

确保 `.env.local` 文件中配置了以下变量:

```env
# TikHub API 配置
TIKHUB_API_BASE_URL=https://api.tikhub.io
TIKHUB_API_KEY=your_tikhub_api_key_here
```

### 2. 获取 API Key

1. 访问 [TikHub 用户中心](https://user.tikhub.io)
2. 注册并登录账户
3. 在用户中心创建 API Token
4. 复制 API Key 并配置到 `.env.local` 文件

### 3. 确认依赖安装

```bash
pnpm install
```

## 输出示例

### 全部垂类低粉爆款榜

```
╔══════════════════════════════════════════════════╗
║     TikHub API - 低粉爆款榜测试工具              ║
╚══════════════════════════════════════════════════╝

=== 测试获取低粉爆款榜列表（全部垂类）===

查询参数:
  时间窗口: 按天
  每页数量: 5
  垂类筛选: 全部

正在获取低粉爆款榜...

✅ 成功获取低粉爆款榜
本页数量: 5
总数: 1000
有更多数据: 是

=== 低粉爆款视频列表 ===

1. 他家👟真靠谱👉@潮鞋聚友（🉐档口） 过个纪念日男朋友的头发被火烧了。
   作者: Yaooo.
   播放: 1.1亿
   点赞: 184.1万
   粉丝: 1.3万
   关注: 1.5万
   热度值: 4155846
   发布时间: 2天前
   视频ID: 7572586014012717994

📄 完整数据已保存到: ./low-fan-list-output.json
```

### 特定垂类低粉爆款榜

```
=== 测试获取低粉爆款榜列表（美食垂类）===

查询参数:
  时间窗口: 按天
  每页数量: 5
  垂类筛选: 美食 (ID: 628)

正在获取低粉爆款榜...

✅ 成功获取低粉爆款榜
本页数量: 4
总数: 1000
有更多数据: 是

=== 美食垂类 - 低粉爆款视频列表 ===

1. 美中不足漏掉了一个，下次继续
   作者: 宁阳广场最辛苦的男人
   播放: 670.3万
   点赞: 6.6万
   粉丝: 3.4万
   关注: 691
   热度值: 458001
   发布时间: 2天前

2. @
   作者: 小怡的雷呀～
   播放: 1108.7万
   点赞: 48.5万
   粉丝: 2.0万
   关注: 8373
   热度值: 410213
   发布时间: 4天前

📄 完整数据已保存到: ./low-fan-list-628-output.json
```

## 输出文件

测试工具会自动生成JSON文件:

### 全部垂类
`low-fan-list-output.json`:
```json
{
  "queryParams": {
    "dateWindow": 24,
    "pageSize": 10,
    "category": "全部"
  },
  "total": 1000,
  "count": 10,
  "hasMore": true,
  "data": {
    "code": 0,
    "data": {
      "page": {...},
      "objs": [...]
    }
  }
}
```

### 特定垂类
`low-fan-list-{tagId}-output.json`:
```json
{
  "queryParams": {
    "dateWindow": 24,
    "pageSize": 5,
    "category": "美食",
    "tagId": 628
  },
  "total": 1000,
  "count": 5,
  "hasMore": true,
  "data": {...}
}
```

## 常见垂类标签ID

| 垂类 | ID | 子垂类示例 |
|------|----|------------|
| 美食 | 628 | 品酒教学(62808), 美食教程(62804) |
| 旅行 | 629 | 旅行风景(62905), 旅行记录(62913) |
| 休闲娱乐 | 634 | 逛街, 现场演出, 运动玩乐 |
| 文化 | 624 | 传统文化, 武术, 人文 |
| 舞蹈 | 612 | 芭蕾舞, 手势舞, 舞蹈教学 |
| 教育校园 | 626 | 高考, 出国留学, 艺考 |
| 汽车 | 635 | 车辆类型, 用车, 新车 |

获取完整标签列表:
```bash
npx tsx scripts/test-content-tags.ts
```

## 应用场景

### 1. 发现新人创作者
- 寻找有潜力的新人账号
- 分析小账号的爆款策略
- 研究低粉账号的成功路径

### 2. 内容趋势分析
- 发现早期趋势信号
- 研究爆款内容的特征
- 分析新兴话题和创意

### 3. 创作灵感
- 学习小账号如何突破
- 找到适合新人的创作方向
- 优化内容创作策略

### 4. 竞品分析
- 研究同垂类低粉爆款
- 分析新人的成功案例
- 发现市场空白机会

### 5. 数据监控
- 定期监控低粉爆款变化
- 跟踪新人创作者趋势
- 预测内容发展方向

## API 费用

根据 TikHub API 定价:
- 每次请求: $0.001 USD
- 仅在返回状态码 200 时计费
- 不缓存，实时获取最新数据

## 故障排查

### 1. 连接失败

**错误**: `TikHub API连接失败`

**解决方法**:
- 检查 `TIKHUB_API_KEY` 是否正确配置
- 确认 API Key 是否有效
- 检查网络连接

### 2. 401 认证错误

**错误**: `错误码: 401`

**解决方法**:
- API Key 无效或已过期
- 重新生成 API Key 并更新 `.env.local`

### 3. 响应数据格式错误

**错误**: `响应数据格式错误`

**解决方法**:
- 检查 API 端点是否正确
- 查看完整响应内容（会在错误时输出）
- 更新类型定义以匹配实际 API 响应

### 4. 空数据列表

**问题**: `本页数量: 0`

**可能原因**:
- 垂类ID不正确
- 该垂类暂时没有低粉爆款视频
- 时间窗口设置不当

**解决方法**:
- 使用 `test-content-tags.ts` 确认正确的垂类ID
- 调整时间窗口参数
- 尝试查询全部垂类（不使用 `--tag` 参数）

## 技术实现

### 核心功能

1. **POST请求支持** - 使用POST方法发送请求体
2. **垂类标签集成** - 支持单个或多个垂类标签查询
3. **数字格式化** - 播放数和点赞数使用万/亿单位
4. **时间格式化** - 显示相对时间（X天前、X小时前）
5. **命令行参数** - 灵活的参数配置

### 设计特点

- ✅ 类型安全 - 完整的 TypeScript 类型定义
- ✅ 错误处理 - 完善的错误捕获和提示
- ✅ 数据格式化 - 美观的数字和时间显示
- ✅ 数据导出 - 自动保存 JSON 格式数据
- ✅ 命令行友好 - 支持参数配置
- ✅ 类型复用 - 复用视频热榜的类型定义

## 相关文件

- **测试脚本**: `scripts/test-low-fan-list.ts`
- **类型定义**: `lib/tikhub/types.ts` (LowFanVideoInfo, GetLowFanListParams, DouyinLowFanListResponse)
- **客户端方法**: `lib/tikhub/client.ts` (getLowFanList)

## 在代码中使用

```typescript
import { getTikHubClient } from '@/lib/tikhub'
import type { HotVideoListTag } from '@/lib/tikhub/types'

async function getLowFanVideosExample() {
  const client = getTikHubClient()

  // 获取全部垂类低粉爆款榜
  const allLowFanVideos = await client.getLowFanList({
    page: 1,
    page_size: 20,
    date_window: 24
  })

  console.log(`获取到 ${allLowFanVideos.data.objs.length} 个低粉爆款视频`)
  console.log(`总数: ${allLowFanVideos.data.page.total}`)

  // 获取美食垂类低粉爆款榜
  const foodLowFanVideos = await client.getLowFanList({
    page: 1,
    page_size: 10,
    date_window: 24,
    tags: [{ value: 628 }]
  })

  // 处理视频数据
  foodLowFanVideos.data.objs.forEach(video => {
    console.log(`${video.item_title}`)
    console.log(`  作者: ${video.nick_name}`)
    console.log(`  播放: ${video.play_cnt}`)
    console.log(`  粉丝: ${video.fans_cnt}`)
    console.log(`  热度: ${video.score}`)
  })
}
```

### 构建查询参数

```typescript
import type { HotVideoListTag } from '@/lib/tikhub/types'

// 单个垂类
const tags1: HotVideoListTag[] = [{ value: 628 }]

// 垂类 + 子垂类
const tags2: HotVideoListTag[] = [
  {
    value: 628,
    children: [
      { value: 62808 }, // 品酒教学
      { value: 62804 }  // 美食教程
    ]
  }
]

// 全部垂类（空数组）
const tagsAll: HotVideoListTag[] = []
```

## 与视频热榜的区别

| 特性 | 低粉爆款榜 | 视频热榜 |
|------|----------|---------|
| 端点 | `fetch_hot_total_low_fan_list` | `fetch_hot_total_video_list` |
| 关注点 | 低粉丝数高播放量 | 全部热门视频 |
| 适用场景 | 发现新人创作者 | 整体趋势分析 |
| 数据特征 | 粉丝数较低（几千到几万） | 粉丝数不限 |
| 典型案例 | 新人一夜爆火 | 头部账号常规内容 |

## 许可证

本项目遵循项目主仓库的许可证。
